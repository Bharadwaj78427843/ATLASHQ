[project]
name = "atlas-backend"
version = "0.1.0"
description = "Atlas Platform Backend Shared Dependencies"
requires-python = ">=3.10"
dependencies = [
    "fastapi[all]>=0.111.0",
    "uvicorn>=0.30.1",
    "sqlalchemy",
    "alembic",
    "pydantic"
]
